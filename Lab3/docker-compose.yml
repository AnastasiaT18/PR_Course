version: "3.9"

services:
  game:
    build: .
    ports:
      - "8080:8080"
    command: npm start 8080 boards/perfect.txt

  simulation:
    build: .
    command: npm run simulation

  tests:
    build: .
    command: npm test
